---
name: example-comps-broken
description: Pull example lease comps for testing. Builds SQL against example_lease_safe and returns rows as JSON.
---

# Example comps (test fixture — broken, description omits sale path)

Handles both sale and lease transaction types in the body, but the
frontmatter description only mentions lease.
