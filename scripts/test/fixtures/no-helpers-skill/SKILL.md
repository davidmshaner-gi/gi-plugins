---
name: example-no-helpers
description: A skill that has no helpers.py — e.g., a pure instruction skill that doesn't call Python.
---

# Example skill (test fixture — no helpers.py)

This skill is just prose instructions for the LLM; there are no helpers.
The contract check must SKIP this skill (pass=true with no issues).
